services:
  # Backend Service (Node.js + Express)
  - type: web
    name: theo-portfolio-api
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 10000
    autoDeploy: true
